version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/db.sqlite
      - N8N_API_KEY=dev-api-key-change-me
    restart: unless-stopped

volumes:
  sqlite_data:
